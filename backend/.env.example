# ===================================
# Vibgyor Chats - Environment Variables
# ===================================

# Google OAuth Configuration
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:8000/auth/google/callback"

# MongoDB Configuration
MONGO_URI="mongodb://localhost:27017"
DB_NAME="VibgyorChats"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_ALGORITHM="HS256"

# Frontend Configuration
FRONTEND_URL="http://localhost:3000"

# CORS Configuration (comma-separated list of allowed origins)
# Add all domains that should be allowed to access the API
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:8000"

# Email Configuration (Gmail SMTP)
MAIL_USERNAME="your-email@gmail.com"
MAIL_PASSWORD="your-gmail-app-password"
MAIL_FROM="Your App Name <your-email@gmail.com>"

# Redis Configuration (for OTP storage)
REDIS_URL="redis://localhost:6379"

# Admin Dashboard Credentials
ADMIN_DASHBOARD_USERNAME="admin"
ADMIN_DASHBOARD_PASSWORD="admin123"

# ===================================
# Notes:
# ===================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to version control
# 3. For Gmail, use App Password (not regular password)
# 4. Change JWT_SECRET to a strong random string in production
# 5. Add all your frontend domains to ALLOWED_ORIGINS (comma-separated)
# 6. Change admin credentials before deploying to production
