# VibgyorChat Frontend Configuration
# Copy this file to .env and update with your actual values

# ============================================
# BACKEND API CONFIGURATION
# ============================================
# Backend API base URL (where your FastAPI server is running)
VITE_API_BASE_URL=http://localhost:8000

# Socket.IO server URL (usually same as API base URL)
VITE_SOCKET_BASE_URL=http://localhost:8000

# ============================================
# FRONTEND CONFIGURATION
# ============================================
# Frontend URL (used for invite links and redirects)
VITE_FRONTEND_URL=http://localhost:3000

# Application name (displayed in UI)
VITE_APP_NAME=VibgyorChat

# ============================================
# ADMIN PANEL CREDENTIALS
# ============================================
# Default admin username for admin panel access
VITE_ADMIN_USERNAME=admin

# Default admin password for admin panel access
VITE_ADMIN_PASSWORD=admin123

# ============================================
# VITE SERVER CONFIGURATION
# ============================================
# Allowed frontend hosts for CORS (comma-separated for multiple hosts)
# Example: host1.tunnelmole.net,host2.ngrok.io,localhost
VITE_ALLOWED_HOSTS=localhost

# ============================================
# DEVELOPMENT NOTES
# ============================================
# 1. All variables with VITE_ prefix are exposed to the browser
# 2. Never commit .env file to version control
# 3. Update VITE_ALLOWED_HOSTS when using tunneling services (ngrok, tunnelmole, etc.)
# 4. For production, use environment-specific values
# 5. Restart dev server after changing .env values

# ============================================
# PRODUCTION EXAMPLE
# ============================================
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_SOCKET_BASE_URL=https://api.yourdomain.com
# VITE_FRONTEND_URL=https://chat.yourdomain.com
# VITE_ALLOWED_HOSTS=chat.yourdomain.com,www.chat.yourdomain.com
